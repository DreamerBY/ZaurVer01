для чего нужен import
Можно использовать полные имена если мы импортируем 2 пакета
с классами которые имеют одинаковые имена.
import java.lang.* бай дефолт импортирует
Поэтому такие классы как string & system импортируются всегда
 java.lang.String st1 = new String("Hello");